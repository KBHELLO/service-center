import { SearchIssuePipe } from './search-issue.pipe';

describe('SearchIssuePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchIssuePipe();
    expect(pipe).toBeTruthy();
  });
});
