import { SearchImeiPipe } from './search-imei.pipe';

describe('SearchImeiPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchImeiPipe();
    expect(pipe).toBeTruthy();
  });
});
